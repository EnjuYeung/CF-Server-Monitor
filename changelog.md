# 最近一次改动

日期：2026-09-19；版本：3.0.0；部署性质：独立二开、全新安装，不兼容原 Workers/D1 数据。

## 代码审查问题修复与职责拆分（2026-09-19，已部署生产）

- 修复凭证修改后旧 JWT 仍有效：凭证、外观增量、告警状态清理与会话版本在同一事务提交，保留已启用 2FA；旧异步请求及密码哈希升级不能覆盖新凭证。成功后清认证 Cookie、断开看板 WS，并要求重新登录。
- 修复关闭通知渠道后把未发送消息标为送达；发送器使用明确结果，未配置时保留待发队列。拆开事件判定、模板/渠道、时间处理及 outbox，消除反向依赖。
- 新增独立 `server_presence.last_seen`，离线告警与看板统计使用认证接收时间，并容忍合法的长上报周期；历史写入节流不再导致在线误判。
- 合并最新上报缓存所有权；单删、批删统一失效历史、延迟、详情、回放和告警窗口，修复同 UUID 重导恢复旧数据。
- 月流量恢复为动态指标；首页每分钟合并完整快照、更新节点集合和订阅，关闭三网详情时也能发现新增节点。
- 新增、编辑、批量编辑、导入共用校验；导入返回逐条错误。排序与批量编辑先完整校验再事务提交，修复部分成功却返回失败的问题。
- 删除 8 个旧公开安装/采集/卸载脚本，共 11,426 行；旧下载 URL 返回 404。原生四目标分发、历史版本归档、采集和兼容迁移源码保留，Agent 版本未变。
- 拆出 `LatestReports`、`ResourceAlertWindows`、节点处理器/服务、前端安装命令与表单工具；删除旧内部 HTTP 调用、空回调、恒真分支和无用缓存 API。计费、探测地址和静态指标定义收敛到 `src/shared/`。
- 修复提交为 `cc0b315`。隔离环境完成 99 项 Node 回归、19 项主控、6 项原生 Agent 和 6 项 Docker 部署验收后，于 2026-09-19 17:13:56 按用户授权部署到 `https://jm.zedy.cc`，未创建 PR。
- 生产沿用原环境、端口和数据卷；15 台节点恢复上报，全部设置、47,922 条切换前历史和 30 个 Agent 归档文件保留，HTTPS/WSS 及桌面/手机页面通过实测。配置、数据和旧镜像已备份，发布证据及回滚位置见 TEST_REPORT.md。

## 后台顶栏与退出按钮右边界对齐（2026-09-17）

- 后台顶栏左右留白统一为 20px，与下方面板一致，消除语言、主题、首页按钮组与退出按钮右边界的 4px 偏差。
- 刷新和退出按钮组使用自动左外边距，在手机宽度下因英文、日文标题换行时仍保持右对齐。
- 已完成隔离浏览器 24 种布局组合、完整回归和主控/原生 Agent 验收，并于 2026-09-17 17:01（Asia/Shanghai）部署到 `https://jm.zedy.cc`。公网已加载对齐样式，HTTPS/WSS 和真实 Agent 更新正常；13 台服务器、设置、3341 条切换前历史和 30 个 Agent 归档文件保留，备份及回滚镜像就绪，详见 TEST_REPORT.md。

## 后台设置与交互精简（2026-09-17）

- 服务器编辑的自动更新开关直接响应勾选，移除单台弹窗和批量保存的自动更新确认；Agent 本地开关与安装命令的行为保持。
- 一键安装命令成功复制后自动关闭弹窗，并在服务器列表短暂显示成功标记。复用带临时文本域的剪贴板回退，复制失败保留命令并提示手动复制。
- 删除服务器弹窗移除 Agent 下载源输入及其状态；卸载命令固定使用当前所选主控的 `/agent`，保留系统、版本类型与安装用户选项。
- 后台服务器列表的未设置/零流量阈值显示 `∞`，点击复制得到同一符号；已设置的阈值继续显示容量及单位。
- 顶栏语言、主题、设置/主页入口统一为 36×36 方形按钮。语言按中/日/英循环，主题按深色/浅色/跟随系统循环，保留本地偏好、键盘操作与中英日提示。
- 通知框的 Bot Token 和 Chat ID 改为上下排列、等宽对齐。管理员登录配置合并到安全设置，与双重验证在同一框内。
- 删除前端 JWT 输入、显隐、验证、提交和相关文案；密钥继续由主控自动生成并持久化。全局 PING 设置仅显示电信、联通、移动、BGP，移除 Node 1–4 的输入、全局保存及对应验证，不覆盖已有节点配置；单台配置和 Agent 协议保持兼容。
- 已于 2026-09-17 16:12（Asia/Shanghai）部署到 `https://jm.zedy.cc`，沿用原环境、端口和数据卷。13 台服务器、设置、2561 条切换前历史和 30 个 Agent 归档文件完整保留，已备份并保留回滚镜像；公网桌面/手机 HTTPS/WSS 和方形按钮切换实测通过，详见 TEST_REPORT.md。

## 延迟与丢包柱固定高度（2026-09-17）

- 首页延迟、丢包的每根柱子统一固定为 10px，参照修改前线上黄色柱约 9.4–10.8px 的高度；删除按数值计算柱高和对应的内联高度样式。
- 绿、蓝、黄、红及缺测灰色的判定、透明度、数字、提示与实时数据更新逻辑保持；缺测和超时柱也使用相同高度。悬停保留提示和高亮，取消纵向放大。
- 桌面/手机的条形、环形卡片、悬停与实时颜色变化已通过 Chromium 验证；更新现有颜色与数据回归断言，架构与代码地图同步。完整验证和部署结果见 TEST_REPORT.md。
- 已构建并替换现有主控镜像，公网桌面/手机柱高均为 10px，HTTPS/WSS 正常；原数据和配置保留，Agent 归档及版本不变。87 项回归、19 项主控和 6 项原生验收通过。

## 柱图对齐、Agent 每日更新与服务改名（2026-09-17）

- 首页丢包柱逐格采用对应延迟柱的高度，丢包颜色、数字和提示保持真实值；两侧标题行统一高度，修复字号差异导致的基线偏移。桌面/手机的条形、环形卡片及实时更新均已实测。
- Agent 发布 `v1.2.0`：仅安装本地 `AUTO_UPDATE=1` 启动自动更新，在启动时和之后每 24 小时检查并安装主控较新版本；未勾选仍手动更新，后台勾选或配置推送不直接改变 VPS 本地开关。中/英/日提示同步说明。
- 已安装程序、服务、PID 与日志名改为 `jan-probe`。升级保留原配置、节点和流量，迁移旧 `cf-probe` 服务；用户服务的旧版自替换通过独立单次任务完成改名；兼容实例锁避免新旧服务重复运行。卸载清理两种名称。
- 配置/流量目录和发布资产名保留旧名称，以兼容现有安装及旧 Agent 的下载协议；新服务名和每日逻辑在 VPS 升级到 v1.2.0 后生效。
- 已重新构建并部署现有 `jan_monitor` 服务，环境、端口和原始数据保留，新增 v1.2.0 四目标归档；旧镜像与数据已备份。87 项 Node 回归、Go/配置测试、19 项主控、6 项原生、6 项 Docker 部署及生产浏览器检查通过；用户服务管理器模拟等边界见 TEST_REPORT.md。

## 生产重新构建与部署（2026-09-17）

- 将当前工作区（含首页持续刷新、后台列表及安装弹窗调整）重新构建为 `server-monitor:local` 并更新现有 `jan_monitor` Compose 服务；沿用 `/opt/1panel/apps/jan_monitor/.env`、现有端口及数据目录。
- 修复原部署 `.env` 缺少可信反代地址导致公网浏览器 WSS 握手 403：根据实际连接将 `TRUSTED_PROXIES` 配置为 `172.20.0.1/32`，其余配置不变。重建后正式域名 HTTPS/WSS、页面渲染和真实节点推送实测通过。
- 保留旧镜像 `server-monitor:rollback-20260917-111530`，在 `/opt/1panel/apps/jan_monitor/backups/redeploy-20260917-111530/` 保存环境文件、数据库一致性快照、GeoIP 库和历史 Agent 文件。生产节点、设置和既有历史记录全部保留；v1.1.0 原归档保持完整，新镜像提供 v1.1.1 四目标分发。
- 完成 87 项 Node 回归、Agent 配置和 Go 测试、19 项主控验收、6 项原生 Agent 验收、5 项 Docker 部署验收及生产浏览器复验；结果与边界见 TEST_REPORT.md。

## 首页持续刷新与后台列表调整（2026-09-17）

- 首页保持打开时持续订阅实时数据，取消后台标签页主动断连、暂停历史补取及连接时长到期自动暂停，保留原采样时间回放节奏。断线按现有 1–30 秒退避持续重试，不再在十次失败后停止；离开首页仍清理连接和计时器。
- 切回、恢复焦点、浏览器解冻、网络恢复和历史页面恢复时立即补取完整最新状态；普通切换不重建健康连接，建立中的连接不重复发起。并发补取合并，旧 REST 响应和旧缓冲样本不能覆盖更新的实时指标；请求失败保留现有服务器列表，列表变化时同步订阅。
- 原 WSS 连接时限配置仅作用于详情页，中/英/日设置文案同步说明。浏览器自身冻结和系统休眠无法由网页取消，恢复后自动补取最新数据。
- 后台服务器列表统一行高及内容垂直居中，修复备注、价格、流量、版本、IP 与复选框的基线偏移；保留长文本省略和小屏横向滚动。
- 复制一键安装命令弹窗删除 Agent 下载源、Agent 版本输入及相关前端状态；命令固定使用所选主控 `/agent` 和默认版本，保留系统及安装用户选项。安装器本身仍支持命令行指定历史版本。
- 最终 87 项 Node 回归、Agent 配置与 Go 测试、19 项主控验收、6 项原生 Agent 验收、5 项 Docker 部署验收及 Chromium 实测通过。详情见 TEST_REPORT.md。

## Agent 构建目标精简（2026-09-17）

- 默认构建和镜像分发从 16 个目标缩减为 4 个：Linux amd64/arm64、FreeBSD amd64/arm64。显式指定已移除目标也会报错；Windows 安装脚本不再打包。
- Agent 版本升为 v1.1.1，避免新产物清单与已归档的 v1.1.0 冲突。保留磁盘历史归档，仅在公开目录、manifest、校验清单和下载入口中过滤不再支持的平台。
- POSIX 安装脚本在下载前拒绝不支持的系统/架构；后台安装与卸载选项移除 macOS/Windows，并显示 amd64/arm64 支持范围。原采集及上报协议保持兼容。
- 增加真实四目标 ELF/HTTP 下载/校验/归档测试、安装脚本平台选择和拒绝路径回归；Docker 部署验收同步为四目标。原生验收要求受支持的运行主机，未具备环境时明确失败，不伪报通过。测试边界与结果见 TEST_REPORT.md。

## 看板延迟图实时更新修复（2026-09-17）

- 修复延迟图只读取首次加载的 `ping/loss` 历史、实时消息仅更新标量值的问题。HTTP/WS 上报经现有看板推送和回放进入对应时间桶，窗口保留 20 桶并随时间滚动；不使用旧值填补空缺。
- 时间桶固定到统一的 6 分钟边界（包含当前未结束桶），避免定期补取历史时最新点跳格；跨桶时立即重建历史缓存。
- 历史成功写入后立即使该服务器的延迟缓存失效；历史点附带 `sample_ts`，用于避免旧回放覆盖新样本。缓存核对数据库实例和采样点数，避免重启或采样参数变化后复用旧窗口。
- 看板每分钟及 WebSocket 重连后补取历史，只更新延迟窗口，保留尚未持久化的新样本；清空历史后不会从旧的前端窗口恢复已删除的持久化样本。
- 延迟数字优先使用当前上报，超时不再沿用历史成功值；柱高按延迟和丢包率变化。空缺显示灰色“无样本”，无丢包数据显示 `--`，不伪造为 0% 或离线。
- 补充缓存、窗口移动、乱序/回放、Vue 响应式与真实 HTTP/WS 验收，结果见 TEST_REPORT.md。更新主控构建后生效，不需要升级 Agent。

## IP 地区库每日自动更新（2026-09-16）

- 主控启动后后台检查 DB-IP Country Lite，此后每 24 小时按 UTC 当前月份检查一次；无需额外定时任务或重建镜像。
- 新库校验通过后原子保存到 `data/geoip/dbip-country-lite.mmdb` 并立即切换内存查询，后续 Agent 上报使用新库；相同内容不重复写盘，拒绝降级到较旧版本。
- 重启选择内置库与持久库中较新的有效版本，持久库损坏可回退内置库。容器重建通过原有数据卷保留已更新的库。
- 下载、解压、数据库校验或写盘失败时保留当前库，次日重试；限制下载时间与体积，并发检查共用任务，主控关闭会取消正在进行的下载。
- 构建下载脚本与运行时复用同一校验及原子写入逻辑；增加真实 HTTP/MMDB/主控更新回归，说明自动更新时机、日志和备份范围。测试结果见 TEST_REPORT.md。

## 原生 Agent 合并与主控分发（2026-09-16）

- 将 cfsm-agent 稳定版 v1.0.16 的完整 Go 源码、测试和 MIT 许可纳入 `agent/`，固定上游提交；本项目 Agent 独立版本从 v1.1.0 开始。
- 保留原有采集、探测、流量、HTTP/WS、配置和平台服务功能。新增 CONTROLLER_URL 名称并兼容 WORKER_URL；Node WS 握手补齐 Date 头，保留时间校准。
- Docker 多阶段构建及 `npm run build:agent` 编译原 Release 全部 16 个目标。最终镜像携带安装脚本、程序与 SHA-256 清单，不包含 Go 编译器。
- 安装、卸载下载、最新版本查询和自动更新改由当前主控 `/agent` 提供，移除对上游 Release 的运行时依赖；支持指定版本及自定义下载源，安装和更新强制校验 SHA-256。
- 主控启动时校验并归档程序到 `data/agent-releases`，镜像替换后保留旧版本；同版本不同产物拒绝覆盖。自动更新开关、6 小时检查和原平台更新/重启流程保持。
- 后台中/英/日安装弹窗改为主控下载源说明，保留各系统与专用用户选项；安装参数统一进行对应 shell 转义。
- 修复 POSIX 下载参数覆盖、发布目录权限及 ARMv5/v6/v7 自动更新文件名匹配；补充原生程序、归档、坏下载、Docker TLS 安装/实际更新/重建/卸载验收。
- 主控 60 项回归、19 项验收、6 项原生 Agent 验收、5 项 Linux 部署验收及双架构镜像检查通过，细节及未覆盖平台见 TEST_REPORT.md。

## 安全入口与双重验证（2026-09-16）

- 域名根路径展示探针首页，匿名用户不再看到设置齿轮或空列表的后台链接。登录后首页恢复齿轮，保留原后台交互；私人看板不再跳转到公开后台地址。
- 新增必填环境变量 `ADMIN_PATH`，支持 8–128 位随机路径段，页面、管理 API 和备份共同使用此入口；旧 `/admin` 返回 404。安全路径不写入公共 HTML/静态构建或匿名配置，敏感响应禁缓存并关闭 Referer。
- 后台安全设置增加 TOTP 双重验证：1Password / Google Authenticator 扫码或手动密钥、六位验证码确认绑定、密码加验证码登录、一次性恢复码和验证后关闭。全部文案支持中/英/日文。
- 密钥使用 AES-256-GCM 加密保存，恢复码仅保存哈希；绑定十分钟过期且绑定当前会话，验证码与恢复码消费采用 SQLite 原子事务，拒绝重放。启用/关闭及路径变更使旧会话失效。
- 登录和 2FA 管理增加 IP/账户限流；设置读取不再返回密码哈希。Compose、Vite 开发代理、接口文档、备份说明及负载脚本同步更新。
- 新增 RFC 测试向量、独立二维码解码和真实 HTTP/WS/SQLite 安全验收，详见 TEST_REPORT.md。

## 全局日文（2026-09-16）

- 增加完整日文词库，右上角增加“日 / 日本語”入口；首页、详情、登录、后台、编辑和批量编辑弹窗支持日文。
- 后台默认语言增加日文，前后端共用语言值校验；设置可保存并在主控重启后保留。浏览器按首选语言顺序选择支持的中/英/日文，手动选择优先并保存在本地。
- 补齐计费周期、36 种货币名称、运行时长、相对时间、图表图例/按钮、加载状态和提示文字；切换语言时同步更新页面 lang 属性及图表图例。
- 日文条形卡片预留更宽的指标标题空间，保证手机端标签与数值不重叠；服务器名称、分组和其他自定义数据不由词库改写。
- 增加词条完整性、占位符、语言选择与计费标签回归；真实验收覆盖日文默认值保存和重启保持。

## 首页排序与分组筛选（2026-09-16）

- 条形图和环形图取消按服务器分组分块，所有视图统一沿用后台保存的服务器顺序；筛选不改变相对顺序。
- 在原分组标题位置增加并排的“分组名 + 数量”按钮，复用国旗筛选样式；分组按首次出现顺序排列，数量统计全部可见服务器。
- 分组采用单选，再次点击取消；可与国旗筛选叠加，切换视图保持条件。空分组归入 Default，无匹配结果显示“暂无数据”。
- 分组按钮支持自动换行、长名称省略及完整名称提示；修复手机单列卡片因内容最小宽度造成的横向溢出。
- 实际浏览器验证交错排序、7/8 台分组数量、组合筛选、隐藏服务器、特殊名称、刷新后的排序、明暗主题及手机布局；详情见 TEST_REPORT.md。

## 界面精简（2026-09-16）

- 删除后台“捐赠支持”页签、独立组件、中英文文案、专用样式及捐赠二维码图片。
- 删除看板“地图”按钮、地图容器、初始化/刷新/主题监听逻辑，以及 Leaflet 和世界地图静态资源。
- 看板保留条形图、环形图和列表；旧浏览器保存的 `map` 偏好自动回退到站点默认视图并更新本地记录。
- 地区识别、地区筛选和国旗展示保持可用；本次验证结果见 TEST_REPORT.md 的界面精简验收。

## 部署与存储

- 将主控迁移到 Node.js 24，统一提供页面、HTTP API、Agent WS 和看板 WS。
- 新增 Dockerfile、Compose bridge 网络、端口映射、持久数据目录、非 root 运行、健康检查、优雅停机和 Nginx 示例。
- 使用 SQLite WAL、同步事务、UUID 数据隔离、滚动 7 天历史及独立最新状态表，数据库限制最多 50 个服务器。
- 删除 Workers/D1/DO 运行时、周分区迁移、Wrangler 与旧平台部署流程；移除 Turnstile 和 Cloudflare 用量配置/UI。

## 功能与兼容

- 保留原 Agent HTTP/WS、schema/MD5 配置协商、流量校正、HTTP 回退与连接时段设置；兼容主控与原生 Agent 同机。
- 使用本地 DB-IP Country Lite 自动识别国家/地区，支持可信代理、同机地址兜底及手动覆盖。
- 后台增加一致性 SQLite 快照下载，提供停机恢复文档；没有自动备份。
- 通知使用持久 outbox，离线、资源、流量和到期事件失败后重试；定时任务在进程内运行。
- 修复前端直达管理页、API origin 令牌隔离、WS 重连计时器，以及首次安装默认用户名未显示导致设置无法保存。
- 修复服务器导入 SQL 占位符数量、Agent Upgrade 版本元数据、告警窗口快照 SQL；清空历史不再删除最新状态，待写旧聚合不会在停机后重新出现。

## P1 修复优先级与结果

| 顺序 | 原问题 | 修复与证据 |
| --- | --- | --- |
| 1 | 匿名 WS 可订阅隐藏服务器 | 握手及推送鉴权、逐节点可见性检查；A11 无泄露 |
| 2 | 并发分配历史分区导致串台 | 删除分区，UUID 外键和联合唯一索引；A03 同时上报隔离 |
| 3 | 写库异常被吞掉，仍确认持久化成功 | 事务回滚并传播错误；A12 HTTP/WS 故障注入均无假成功 |
| 4 | 旧设置迁移可能覆盖私人站点配置 | 全新设置模型、删除旧迁移入口、读取失败不回退默认公开；A11 私人模式、A15 重启 |
| 5 | 通知先标已发，失败后无法补发 | 状态与 outbox 原子入队、持久重试；A09、A10、A15 |

## 文档与验收

新增 AGENTS.md（500 字以内）、architecture.md、code_map.md、TEST_REPORT.md、changelog.md；testing.md 保留用户指定原文。重写部署、API、主题和测试文档。新增真实 HTTP/WS/SQLite 验收、边界负载测试、代理验证及构建 CI；最近结果与未覆盖项见 TEST_REPORT.md。

## 后续优先级

- P2：继续拆分较大的 RealtimeHub、admin 处理器和设置模块，统一重复的最新上报缓存；保持本次协议回归覆盖。
- P2：按实际 VPS 配置做长时间运行与大历史库测试，并使用测试账号逐个验证外部通知渠道。
- P3：按实际使用的第三方主题补充端到端验收与布局回归。

本次没有实现自动备份、多主控副本、旧数据迁移；这些项目不属于首版范围。
