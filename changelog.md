# 最近一次改动

日期：2026-09-16；版本：3.0.0；部署性质：独立二开、全新安装，不兼容原 Workers/D1 数据。

## IP 地区库每日自动更新（2026-09-16）

- 主控启动后后台检查 DB-IP Country Lite，此后每 24 小时按 UTC 当前月份检查一次；无需额外定时任务或重建镜像。
- 新库校验通过后原子保存到 `data/geoip/dbip-country-lite.mmdb` 并立即切换内存查询，后续 Agent 上报使用新库；相同内容不重复写盘，拒绝降级到较旧版本。
- 重启选择内置库与持久库中较新的有效版本，持久库损坏可回退内置库。容器重建通过原有数据卷保留已更新的库。
- 下载、解压、数据库校验或写盘失败时保留当前库，次日重试；限制下载时间与体积，并发检查共用任务，主控关闭会取消正在进行的下载。
- 构建下载脚本与运行时复用同一校验及原子写入逻辑；增加真实 HTTP/MMDB/主控更新回归，说明自动更新时机、日志和备份范围。测试结果见 TEST_REPORT.md。

## 原生 Agent 合并与主控分发（2026-09-16）

- 将 cfsm-agent 稳定版 v1.0.16 的完整 Go 源码、测试和 MIT 许可纳入 `agent/`，固定上游提交；本项目 Agent 独立版本从 v1.1.0 开始。
- 保留原有采集、探测、流量、HTTP/WS、配置和平台服务功能。新增 CONTROLLER_URL 名称并兼容 WORKER_URL；Node WS 握手补齐 Date 头，保留时间校准。
- Docker 多阶段构建及 `npm run build:agent` 编译原 Release 全部 16 个目标。最终镜像携带安装脚本、程序与 SHA-256 清单，不包含 Go 编译器。
- 安装、卸载下载、最新版本查询和自动更新改由当前主控 `/agent` 提供，移除对上游 Release 的运行时依赖；支持指定版本及自定义下载源，安装和更新强制校验 SHA-256。
- 主控启动时校验并归档程序到 `data/agent-releases`，镜像替换后保留旧版本；同版本不同产物拒绝覆盖。自动更新开关、6 小时检查和原平台更新/重启流程保持。
- 后台中/英/日安装弹窗改为主控下载源说明，保留各系统与专用用户选项；安装参数统一进行对应 shell 转义。
- 修复 POSIX 下载参数覆盖、发布目录权限及 ARMv5/v6/v7 自动更新文件名匹配；补充原生程序、归档、坏下载、Docker TLS 安装/实际更新/重建/卸载验收。
- 主控 60 项回归、19 项验收、6 项原生 Agent 验收、5 项 Linux 部署验收及双架构镜像检查通过，细节及未覆盖平台见 TEST_REPORT.md。

## 安全入口与双重验证（2026-09-16）

- 域名根路径展示探针首页，匿名用户不再看到设置齿轮或空列表的后台链接。登录后首页恢复齿轮，保留原后台交互；私人看板不再跳转到公开后台地址。
- 新增必填环境变量 `ADMIN_PATH`，支持 8–128 位随机路径段，页面、管理 API 和备份共同使用此入口；旧 `/admin` 返回 404。安全路径不写入公共 HTML/静态构建或匿名配置，敏感响应禁缓存并关闭 Referer。
- 后台安全设置增加 TOTP 双重验证：1Password / Google Authenticator 扫码或手动密钥、六位验证码确认绑定、密码加验证码登录、一次性恢复码和验证后关闭。全部文案支持中/英/日文。
- 密钥使用 AES-256-GCM 加密保存，恢复码仅保存哈希；绑定十分钟过期且绑定当前会话，验证码与恢复码消费采用 SQLite 原子事务，拒绝重放。启用/关闭及路径变更使旧会话失效。
- 登录和 2FA 管理增加 IP/账户限流；设置读取不再返回密码哈希。Compose、Vite 开发代理、接口文档、备份说明及负载脚本同步更新。
- 新增 RFC 测试向量、独立二维码解码和真实 HTTP/WS/SQLite 安全验收，详见 TEST_REPORT.md。

## 全局日文（2026-09-16）

- 增加完整日文词库，右上角增加“日 / 日本語”入口；首页、详情、登录、后台、编辑和批量编辑弹窗支持日文。
- 后台默认语言增加日文，前后端共用语言值校验；设置可保存并在主控重启后保留。浏览器按首选语言顺序选择支持的中/英/日文，手动选择优先并保存在本地。
- 补齐计费周期、36 种货币名称、运行时长、相对时间、图表图例/按钮、加载状态和提示文字；切换语言时同步更新页面 lang 属性及图表图例。
- 日文条形卡片预留更宽的指标标题空间，保证手机端标签与数值不重叠；服务器名称、分组和其他自定义数据不由词库改写。
- 增加词条完整性、占位符、语言选择与计费标签回归；真实验收覆盖日文默认值保存和重启保持。

## 首页排序与分组筛选（2026-09-16）

- 条形图和环形图取消按服务器分组分块，所有视图统一沿用后台保存的服务器顺序；筛选不改变相对顺序。
- 在原分组标题位置增加并排的“分组名 + 数量”按钮，复用国旗筛选样式；分组按首次出现顺序排列，数量统计全部可见服务器。
- 分组采用单选，再次点击取消；可与国旗筛选叠加，切换视图保持条件。空分组归入 Default，无匹配结果显示“暂无数据”。
- 分组按钮支持自动换行、长名称省略及完整名称提示；修复手机单列卡片因内容最小宽度造成的横向溢出。
- 实际浏览器验证交错排序、7/8 台分组数量、组合筛选、隐藏服务器、特殊名称、刷新后的排序、明暗主题及手机布局；详情见 TEST_REPORT.md。

## 界面精简（2026-09-16）

- 删除后台“捐赠支持”页签、独立组件、中英文文案、专用样式及捐赠二维码图片。
- 删除看板“地图”按钮、地图容器、初始化/刷新/主题监听逻辑，以及 Leaflet 和世界地图静态资源。
- 看板保留条形图、环形图和列表；旧浏览器保存的 `map` 偏好自动回退到站点默认视图并更新本地记录。
- 地区识别、地区筛选和国旗展示保持可用；本次验证结果见 TEST_REPORT.md 的界面精简验收。

## 部署与存储

- 将主控迁移到 Node.js 24，统一提供页面、HTTP API、Agent WS 和看板 WS。
- 新增 Dockerfile、Compose bridge 网络、端口映射、持久数据目录、非 root 运行、健康检查、优雅停机和 Nginx 示例。
- 使用 SQLite WAL、同步事务、UUID 数据隔离、滚动 7 天历史及独立最新状态表，数据库限制最多 50 个服务器。
- 删除 Workers/D1/DO 运行时、周分区迁移、Wrangler 与旧平台部署流程；移除 Turnstile 和 Cloudflare 用量配置/UI。

## 功能与兼容

- 保留原 Agent HTTP/WS、schema/MD5 配置协商、流量校正、HTTP 回退与连接时段设置；兼容主控与原生 Agent 同机。
- 使用本地 DB-IP Country Lite 自动识别国家/地区，支持可信代理、同机地址兜底及手动覆盖。
- 后台增加一致性 SQLite 快照下载，提供停机恢复文档；没有自动备份。
- 通知使用持久 outbox，离线、资源、流量和到期事件失败后重试；定时任务在进程内运行。
- 修复前端直达管理页、API origin 令牌隔离、WS 重连计时器，以及首次安装默认用户名未显示导致设置无法保存。
- 修复服务器导入 SQL 占位符数量、Agent Upgrade 版本元数据、告警窗口快照 SQL；清空历史不再删除最新状态，待写旧聚合不会在停机后重新出现。

## P1 修复优先级与结果

| 顺序 | 原问题 | 修复与证据 |
| --- | --- | --- |
| 1 | 匿名 WS 可订阅隐藏服务器 | 握手及推送鉴权、逐节点可见性检查；A11 无泄露 |
| 2 | 并发分配历史分区导致串台 | 删除分区，UUID 外键和联合唯一索引；A03 同时上报隔离 |
| 3 | 写库异常被吞掉，仍确认持久化成功 | 事务回滚并传播错误；A12 HTTP/WS 故障注入均无假成功 |
| 4 | 旧设置迁移可能覆盖私人站点配置 | 全新设置模型、删除旧迁移入口、读取失败不回退默认公开；A11 私人模式、A15 重启 |
| 5 | 通知先标已发，失败后无法补发 | 状态与 outbox 原子入队、持久重试；A09、A10、A15 |

## 文档与验收

新增 AGENTS.md（500 字以内）、architecture.md、code_map.md、TEST_REPORT.md、changelog.md；testing.md 保留用户指定原文。重写部署、API、主题和测试文档。新增真实 HTTP/WS/SQLite 验收、边界负载测试、代理验证及构建 CI；最近结果与未覆盖项见 TEST_REPORT.md。

## 后续优先级

- P2：继续拆分较大的 RealtimeHub、admin 处理器和设置模块，统一重复的最新上报缓存；保持本次协议回归覆盖。
- P2：按实际 VPS 配置做长时间运行与大历史库测试，并使用测试账号逐个验证外部通知渠道。
- P3：按实际使用的第三方主题补充端到端验收与布局回归。

本次没有实现自动备份、多主控副本、旧数据迁移；这些项目不属于首版范围。
